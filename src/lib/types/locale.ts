export type Dictionary = {
    appName: string,
    components: {
        header: {
            title: string,
            api: string,
            github: string,
        },
        footer: {
            credits: string,
        }
    }
}