export type AppTheme = {
    colors: {
        primary: string,
        secondary: string,
        background: string,
        foreground: string,
        typography: string,
        border: string,
        error: string
    }
}